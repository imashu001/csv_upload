<div>
  <% if(fileFormat && fileFormat.length > 0) { let keys =
  Object.keys(fileFormat[0]); %>
  <table>
    <caption>
      <h2><%= title %></h2>
    </caption>
    <thead>
      <tr>
        <% for(let i = 0; i < keys.length; i++){ %>
        <th><%= keys[i] %></th>
        <% } %>
      </tr>
    </thead>
    <tbody>
      <% for(jsonObj of fileFormat){ %>
      <tr>
        <% for (key in jsonObj) { %>
        <td><%= jsonObj[key] %></td>
        <% } %>
      </tr>
      <% } %>
    </tbody>
  </table>
  <% }%>
</div>
